db:
  name:
    local: localdb
    ${env}: ${env}_db
  type: "dynamodb"
  endpoint:
    local: "http://docker.for.mac.localhost:8000"
  credential:
    local: "local-mode"
  entities:
    messages:
      local:
        tablename: Messages
      ${env}:
        tablename: ${env}_Messages
